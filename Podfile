# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GeoFirestoreIOS' do
  use_frameworks!

  # Pods for GeoFirestoreIOS
  pod 'Firebase/Firestore'

    target 'GeoFirestoreIOSTests' do
        inherit! :search_paths

        # Pods for testing
        pod 'Firebase/Firestore'
    end

end


